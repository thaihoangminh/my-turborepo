services:
  docs-development:
    build:
      context: ../../
      dockerfile: docker/development/docs.dockerfile
      secrets:
        - TURBO_TOKEN
    environment:
      TURBO_TEAM: ${TURBO_TEAM}
    image: docs-development:latest
    ports:
      - "3001:3000"
